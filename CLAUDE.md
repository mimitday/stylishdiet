# Claude Code Working Rules

このリポジトリでは、Claude Code は以下のルールに従うこと。

---

# 最優先

- 勝手にコードを書き始めない
- 実装前に前提・対象ファイル・変更範囲を確認する
- 「何を変えるか」を短く整理してから作業する
- ユーザー承認前に実装しない

---

# 実装方針

- 過剰実装しない
- 最小構成で解決する
- 関係ないコードを触らない
- 不必要なリファクタをしない
- デザイン変更を勝手に行わない
- 文言を勝手に変更しない
- 既存コメントを勝手に消さない
- 命名規則を勝手に変更しない
- ファイル構成を勝手に変更しない

---

# コードを書く前にやること

実装前に必ず：

1. 周辺コードを読む
2. 呼び出し元を確認
3. 共通関数を確認
4. 既存パターンを確認
5. どの実装パターンを採用するか明示

矛盾する実装パターンを混ぜない。

---

# AIとコードの責務分離

AIがやること：
- UI提案
- 文言提案
- 構成提案
- 判断が必要な部分
- 問題点整理
- 差分整理

コード側でやること：
- ルーティング
- リトライ
- 状態管理
- 決定論的処理
- 単純反復処理

---

# 作業ルール

- 長くなったら要約して仕切り直す
- 各ステップごとにチェックポイントを置く
- 不確実な成功を成功扱いしない
- 未確認事項は未確認と明示する
- エラーを握り潰さない
- 「多分OK」で終了しない

---

# テスト

テストは：

- 挙動確認だけでなく
- 「なぜその実装なのか」
- 「意図通りか」

まで確認する。

---

# Git運用

- git add / commit / push はユーザーが決定した内容のみ
- 勝手にcommitしない
- 勝手にpushしない
- force push禁止
- 破壊操作禁止

以下は禁止：

- git reset --hard
- git clean -fd
- 強制checkout
- 履歴破壊
- branch削除
- revert乱発

---

# ファイル操作

- 既存ファイルを削除する前に確認
- 上書き前に変更範囲を明示
- assetsのパスを勝手に変えない
- ファイル名の大文字小文字を変更しない

GitHub Pages は case sensitive であることを前提に扱う。

例：
- logo.PNG
- logo.png

は別物。

---

# HTML出力ルール

HTML修正時：

- 一部分だけではなく完成ファイル全体を出力
- コピペ断片ではなく完成形を出力
- ダウンロード可能形式を優先
- 「既存コード省略」は禁止

---

# UI/UXルール

- ADHD/脳疲労前提
- 情報量を増やしすぎない
- 一画面で完結できるUIを優先
- 選択肢を増やしすぎない
- 「考えなくていいUX」を優先

---

# デザインルール

- AIっぽいUIを避ける
- シンプル優先
- 余白を大事にする
- 実装コストより体験を優先
- ただし過剰演出は禁止

---

# PWA/Pagesルール

- GitHub Pages前提
- HTML/CSS/Vanilla JS優先
- React化を勝手に行わない
- manifest / service worker を壊さない
- 相対パスを優先

# 記載ルール

- 既存ルールを勝手に統合しない
- 要約しない
- リライトしない
- 「より良い構成」に再編しない
- 追記形式を優先する

# 出力ルール

回答には必要に応じて：

- 前提
- 変更内容
- 成功条件
- 未確認事項
- 残タスク
- 次にやること

を簡潔に含める。


差分ではなく、
全文入り完成版を再出力してください。
---

# 目的

目的は：

「人間の負荷を減らしながら、
小さく確実に前進すること」

である。

速度より、
認知負荷軽減と事故防止を優先する。
